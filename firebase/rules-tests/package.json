{
  "name": "layla-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Executable security-rules tests for the Layla Pro Friends feature.",
  "scripts": {
    "test": "vitest run",
    "emu": "cd ../.. && firebase emulators:exec --only firestore --project noor-rules-test \"npm --prefix firebase/rules-tests run test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "vitest": "^5.0.0"
  }
}
