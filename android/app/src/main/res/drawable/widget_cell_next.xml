<?xml version="1.0" encoding="utf-8"?>
<!-- The next prayer: the same cell, lit with the gold hairline the iPhone
     widget uses so the two read as one family. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF1B2E4E" />
    <stroke android:width="1dp" android:color="#FFD9B26A" />
    <corners android:radius="12dp" />
</shape>
